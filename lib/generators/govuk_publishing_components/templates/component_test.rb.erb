require 'component_test_helper'

class <%= @test_name %>Test < ComponentTestCase
  def component_name
    "<%= @public_name %>"
  end

  test "fails to render when the test has not been written" do
    raise Exception
  end
end
