export declare class EventLoopSpinner {
    private thresholdMs;
    private afterLastSpin;
    constructor(thresholdMs?: number);
    reset(): void;
    isStarving(): boolean;
    spin(): Promise<void>;
}
